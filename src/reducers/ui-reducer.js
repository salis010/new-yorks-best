import * as actionTypes from '../constants/action-types'

const initialState = {
    loading: false,
}

export const uiReducer = (state = initialState, action)=> {
    switch(action.type) {
        default:
            return state
    }
}